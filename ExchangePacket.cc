ExchangePacket(Ptr<Node> node1, Ptr<Node> node2){
	uint32_t node1_id;
	uint32_t node2_id;
	
	node1_id = node1->GetId();
	node2_id = node2->GetId();
	//Compare the Ad list and exchange 

	//sign check process

	//Trade checks

	//Trade ad packets
}
